---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex gap-2">
  {tags.map((tag) => (
    <a
      href={`/tags/${tag}`}
      class="text-sm px-3 py-1 text-primary-700 hover:text-primary-600 bg-gray-50 rounded-full hover:bg-gray-100"
    >
      #{tag}
    </a>
  ))}
</div>
